# Test Suite

This directory is reserved for TankTrouble AI Train regression and environment tests.

The first tests should cover deterministic game-state reset, valid actions, shell collisions, reward calculation, and reproducible seeded episodes.
